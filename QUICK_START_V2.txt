╔═══════════════════════════════════════════════════════════════════════════╗
║                    ☕ CAFE ORDERING SYSTEM v2                              ║
║                        QUICK REFERENCE GUIDE                               ║
╚═══════════════════════════════════════════════════════════════════════════╝

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🚀 START SERVER
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  cd /Users/hnt_4/GitCloneDestination/SOAFinal
  source .venv/bin/activate
  python3 app_v2.py

  Server: http://localhost:3000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
✨ FEATURES IMPLEMENTED
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  ✅ User Accounts
     • OTP-based registration via email
     • Secure password hashing
     • User profile storage

  ✅ Order Management
     • Create orders with multiple items
     • Size-based pricing (S: 90%, M: 100%, L: 110%)
     • Order status tracking
     • Special notes for customization

  ✅ Order History
     • View all past orders
     • Filter by status
     • View order details

  ✅ Favorites
     • Add/remove favorite items
     • Quick access from sidebar
     • Persistent storage

  ✅ Search & Browse
     • Full-text search on menu
     • Category filtering
     • Price display

  ✅ Promo Codes
     • Validate discount codes
     • Apply discount to total
     • Track usage

  ✅ Email Notifications
     • Order confirmation emails
     • OTP delivery (with Gmail setup)
     • HTML formatted templates

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📋 DATABASE SCHEMA
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  users
  ├── id (TEXT, PRIMARY KEY)
  ├── email (TEXT, UNIQUE)
  ├── password_hash (TEXT)
  ├── full_name (TEXT)
  ├── phone (TEXT)
  └── created_at (TIMESTAMP)

  otp_codes
  ├── id (INTEGER, PRIMARY KEY)
  ├── email (TEXT)
  ├── code (TEXT)
  ├── expires_at (TIMESTAMP)
  └── verified (BOOLEAN)

  orders
  ├── id (TEXT, PRIMARY KEY)
  ├── user_id (TEXT, FOREIGN KEY)
  ├── items (JSON)
  ├── total (REAL)
  ├── status (TEXT)
  ├── special_notes (TEXT)
  ├── promo_code (TEXT)
  ├── discount (REAL)
  ├── payment_method (TEXT)
  ├── customer_name (TEXT)
  ├── customer_phone (TEXT)
  └── created_at (TIMESTAMP)

  favorites
  ├── id (INTEGER, PRIMARY KEY)
  ├── user_id (TEXT, FOREIGN KEY)
  ├── product_id (TEXT)
  └── added_at (TIMESTAMP)

  promo_codes
  ├── id (TEXT, PRIMARY KEY)
  ├── code (TEXT, UNIQUE)
  ├── discount_percent (REAL)
  ├── max_uses (INTEGER)
  ├── used_count (INTEGER)
  ├── expires_at (TIMESTAMP)
  └── created_at (TIMESTAMP)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🎯 USER WORKFLOWS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  WORKFLOW 1: New User Registration
  ─────────────────────────────────────
  1. Click "Register" on login screen
  2. Enter email → "Send OTP Code"
  3. Check email for 6-digit code
  4. Enter OTP, name, phone
  5. Click "Create Account"
  6. Redirected to app as logged-in user

  WORKFLOW 2: Place Order
  ─────────────────────────────────────
  1. Browse menu (all/category/search)
  2. Click "Add" on product
  3. Select size (S/M/L)
  4. Adjust quantity in cart
  5. Click "Checkout"
  6. Fill in details & payment method
  7. Apply promo code (optional)
  8. Click "Place Order"
  9. Get order ID confirmation
  10. View in "History" tab

  WORKFLOW 3: Track Favorites
  ─────────────────────────────────────
  1. Click 🤍 on any product (becomes ❤️)
  2. View in "❤️ Favorites" sidebar
  3. View full list in "Favorites" tab
  4. Click to add to cart quickly
  5. Click ❤️ again to remove

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
⚙️ API ENDPOINTS
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Authentication:
  ───────────────
  POST   /api/auth/send-otp            Send OTP to email
  POST   /api/auth/verify-otp          Register/verify user
  POST   /api/auth/login               Login with password

  Menu:
  ─────
  GET    /api/menu                     Get all items
  GET    /api/menu/{category}          Get by category
  GET    /api/menu/search?q=...        Search items

  Favorites:
  ──────────
  POST   /api/favorites/add            Add to favorites
  GET    /api/favorites                Get user favorites
  DELETE /api/favorites/{product_id}   Remove from favorites

  Orders:
  ───────
  POST   /api/checkout                 Create order
  GET    /api/orders                   Get order history
  GET    /api/orders/{order_id}        Get order details
  PUT    /api/orders/{order_id}/status Update order status

  Promo:
  ──────
  POST   /api/promo/validate           Validate promo code

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🧪 TESTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Run comprehensive feature tests:
  ────────────────────────────────
  source .venv/bin/activate
  python3 test_features.py

  Manual testing in browser:
  ──────────────────────────
  1. Open http://localhost:3000
  2. Register with email
  3. Search for "latte"
  4. Add to favorites
  5. Add items to cart
  6. Apply promo code (TEST10)
  7. Checkout with special notes
  8. View order history

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📧 GMAIL SETUP (Optional)
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  1. Go to: https://myaccount.google.com/security
  2. Enable "2-Step Verification"
  3. Go to: https://myaccount.google.com/apppasswords
  4. Select: Mail, Windows Computer
  5. Copy 16-character password
  6. Create .env file:

     GMAIL_ADDRESS=your-email@gmail.com
     GMAIL_PASSWORD=your-app-password

  7. Restart server

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🔍 FILE REFERENCE
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  app_v2.py                      Backend server (FastAPI + SQLite)
  order_frontend_v2.html         Frontend UI (HTML5 + CSS3 + JS)
  order_frontend.html            Symlink to v2
  cafe_orders.db                 SQLite database
  requirements.txt               Python dependencies
  .env.example                   Gmail configuration template
  test_features.py               Feature test suite
  README_V2_COMPLETE.md          Full documentation
  QUICK_START_V2.txt             This file

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
🆘 TROUBLESHOOTING
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Port 3000 already in use?
  ────────────────────────
  lsof -i :3000 | tail -n +2 | awk '{print $2}' | xargs -r kill -9

  Email not configured?
  ─────────────────────
  App works fine! Emails just won't send (demo mode)

  OTP code invalid?
  ────────────────
  - Request new code (old one expires in 10 minutes)
  - Without Gmail setup, OTP can't actually be sent
  - Use test credentials for demo

  Database issues?
  ────────────────
  rm cafe_orders.db
  python3 app_v2.py  (recreates clean DB)

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
📊 TECH STACK
━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

  Backend:     FastAPI (Python 3.13)
  Frontend:    HTML5 + CSS3 + Vanilla JavaScript
  Database:    SQLite (file-based)
  Email:       SMTP via Gmail
  API:         RESTful with CORS
  Port:        3000

━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

Questions? See README_V2_COMPLETE.md for detailed documentation.

Happy ordering! ☕🎉
