version: '3.8'

services:
  cafe-ordering:
    build: .
    container_name: cafe-ordering-system
    ports:
      - "3000:3000"
    environment:
      - GMAIL_ADDRESS=huynhnhattien0411@gmail.com
      - GMAIL_PASSWORD=rqdbmyhpfglksotn
    volumes:
      - ./data:/app/data
      - ./backups:/app/backups
      - ./cafe_orders.db:/app/cafe_orders.db
      # Mount frontend files for hot reload (dev only)
      - ./frontend:/app/frontend
      - ./index.html:/app/index.html
      - ./app.py:/app/app.py
      - ./database.py:/app/database.py
      - ./routers:/app/routers
      - ./models:/app/models
      - ./utils:/app/utils
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:3000/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: unless-stopped
    networks:
      - cafe-network

networks:
  cafe-network:
    driver: bridge
